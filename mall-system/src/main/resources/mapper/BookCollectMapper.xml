<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.platform.mapper.BookCollectMapper">
    <select id="queryPage" resultType="com.project.platform.entity.BookCollect">
        SELECT bc.*, b.book_name AS bookName, b.main_img AS bookMainImg, b.price AS bookPrice
        FROM book_collect bc
        LEFT JOIN book b ON bc.book_id = b.id
        <where>
            <if test="query.userId != null and query.userId != 0">
                AND bc.user_id = #{query.userId}
            </if>
        </where>
        ORDER BY bc.id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="queryCount" resultType="int">
        SELECT count(bc.id) FROM book_collect bc
        <where>
            <if test="query.userId != null and query.userId != 0">
                AND bc.user_id = #{query.userId}
            </if>
        </where>
    </select>
</mapper>
