<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.platform.mapper.OrderEvaluateMapper">
    <select id="queryPage" resultType="com.project.platform.entity.OrderEvaluate">
        SELECT oe.*, u.nickname AS userName, b.book_name AS bookName
        FROM order_evaluate oe
        LEFT JOIN user u ON oe.user_id = u.id
        LEFT JOIN book b ON oe.book_id = b.id
        ORDER BY oe.id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="queryCount" resultType="int">
        SELECT count(id) FROM order_evaluate
    </select>
</mapper>
